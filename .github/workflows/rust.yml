name: Rust

on: [push, pull_request]

jobs:
  Unix:
    name: ${{ matrix.name }} (${{ matrix.config }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-16.04, ubuntu-18.04, ubuntu-20.04, macos-latest]
        config: [Debug, Release]
        include:
          - os: macos-latest
            name: macOS
          - os: ubuntu-16.04
            name: Linux
          - os: ubuntu-18.04
            name: Linux
          - os: ubuntu-20.04
            name: Linux
    steps:
    - uses: actions/checkout@v2
    - uses: actions-rs/toolchain@v1
      with:
          toolchain: stable    
    - name: Dependencies (Linux)
      if: runner.os == 'Linux'
      run: sudo apt-get install xorg-dev libxcb-render0-dev
    - name: build
      uses: actions-rs/cargo@v1
      with:
        command: build
    - name: test
      uses: actions-rs/cargo@v1
      with:
        command: test
    - name: lint
      uses: actions-rs/clippy-check@v1
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
